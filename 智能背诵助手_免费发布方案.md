# 智能背诵助手 - 免费发布方案

## 一、免费托管平台对比

| 平台 | 带宽 | 构建次数 | 自定义域名 | 中国访问 | 推荐度 |
|------|------|---------|-----------|---------|--------|
| **Cloudflare Pages** | 无限 | 500次/月 | ✅ 免费SSL | ⚠️ 需配置 | ⭐⭐⭐⭐⭐ |
| **Vercel** | 100GB/月 | 100次/天 | ✅ 免费SSL | ⚠️ 稍慢 | ⭐⭐⭐⭐ |
| **Netlify** | 100GB/月 | 300分钟/月 | ✅ 免费SSL | ⚠️ 稍慢 | ⭐⭐⭐ |
| **GitHub Pages** | 1GB | 无限制 | ❌ 需付费 | ❌ 国内慢 | ⭐⭐ |

**推荐首选：Cloudflare Pages** - 无限带宽，国内访问速度快

---

## 二、免费发布方案（Cloudflare Pages）

### 2.1 方案优势

```
✅ 无限带宽
✅ 免费SSL证书
✅ 全球CDN加速
✅ 自定义域名
✅ 自动构建部署
✅ 中国大陆访问优化
✅ 不用备案！
```

### 2.2 部署步骤

#### Step 1: 准备代码（已有）
```
智能背诵助手/
├── index.html
├── styles.css
├── app.js
└── README.md
```

#### Step 2: 上传到GitHub
```bash
# 1. 创建GitHub仓库
访问 https://github.com 创建新仓库 "recitation-helper"

# 2. 本地初始化（如果没有git）
cd 智能背诵助手
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/recitation-helper.git
git push -u origin main
```

#### Step 3: 部署到Cloudflare Pages
```
1. 注册 Cloudflare: https://dash.cloudflare.com
2. 选择 "Workers & Pages" → "Create application"
3. 选择 "Pages" → "Connect to Git"
4. 选择你的GitHub仓库
5. 设置构建命令（静态网站留空）
6. 点击 "Deploy site"
7. 等待部署完成，获得免费域名: xxx.pages.dev
```

#### Step 4: 绑定自定义域名（可选）
```
1. 在Cloudflare中添加你的域名
2. DNS解析指向Cloudflare Pages
3. 自动获得免费SSL证书
4. 国内访问速度优化
```

---

## 三、需要后端服务的替代方案

### 3.1 问题分析

当前原型依赖的服务：
| 功能 | 原型实现 | 免费替代方案 |
|------|---------|-------------|
| OCR识别 | 模拟 | 百度/腾讯API（免费额度） |
| 语音合成 | 浏览器TTS | 浏览器原生API（免费） |
| 语音识别 | 模拟 | 浏览器原生API（免费） |
| 数据存储 | LocalStorage | 需要后端 |

### 3.2 完全免费方案架构

```
┌─────────────────────────────────────────┐
│         Cloudflare Pages (免费托管)       │
│   ┌─────────────────────────────────┐   │
│   │  前端应用 (HTML/CSS/JS)          │   │
│   └─────────────────────────────────┘   │
│                   │                      │
│                   ▼                      │
│   ┌─────────────────────────────────┐   │
│   │  Cloudflare KV (免费数据库)      │   │
│   │  - 用户数据存储                  │   │
│   │  - 背诵记录                      │   │
│   └─────────────────────────────────┘   │
│                   │                      │
│                   ▼                      │
│   ┌─────────────────────────────────┐   │
│   │  Cloudflare Workers (免费API)   │   │
│   │  - 第三方API代理                │   │
│   │  - 业务逻辑                     │   │
│   └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
                   │
                   ▼
        ┌─────────────────────┐
        │   第三方免费服务      │
        │  - 百度AI OCR        │
        │  - 腾讯云语音        │
        └─────────────────────┘
```

### 3.3 Cloudflare免费额度

| 服务 | 免费额度 | 说明 |
|------|---------|------|
| Pages | 无限站数/无限带宽 | 500次构建/月 |
| KV | 1GB存储/10万次读写 | 适合小型应用 |
| Workers | 10万次/天 | 适合API处理 |
| R2 | 10GB存储 | 图片存储 |

---

## 四、进阶：白嫖完整功能方案

### 4.1 推荐组合

```
前端: Cloudflare Pages (免费托管)
API: Cloudflare Workers (免费计算)
存储: Cloudflare KV (免费键值存储)
OCR: 百度AI (5万次/天免费)
语音: 浏览器原生API (完全免费)
域名: xxx.pages.dev (免费) 或 自购域名
```

### 4.2 月度成本

| 项目 | 费用 |
|------|------|
| 托管 | ¥0 |
| 数据库 | ¥0 |
| OCR | ¥0（免费额度内） |
| 语音 | ¥0（浏览器原生） |
| 域名 | ¥0（pages.dev免费） |
| **总计** | **¥0** |

---

## 五、部署文档

### 5.1 快速部署脚本

创建 `deploy.sh`：
```bash
#!/bin/bash
# Cloudflare Pages 部署脚本

# 安装wrangler CLI
npm install -g wrangler

# 登录Cloudflare
wrangler login

# 部署到Cloudflare Pages
wrangler pages deploy . --project-name=recitation-helper
```

### 5.2 遇到的坑和解决方案

| 问题 | 解决方案 |
|------|---------|
| 部署失败 | 检查 `wrangler.toml` 配置 |
| 跨域问题 | Cloudflare Workers代理 |
| KV访问慢 | 使用 Durable Objects |
| 中国访问慢 | 绑定国内备案域名 |

---

## 六、推荐：渐进式升级路径

### 第一阶段（立即上线，¥0）
```
Cloudflare Pages 托管
├── 静态页面部署
├── KV存储（用户记录）
└── 浏览器原生语音/OCR
```

### 第二阶段（增长后，¥50/月）
```
升级到付费套餐
├── 自定义域名（需备案）
├── 更多KV存储
└── 优先边缘计算
```

### 第三阶段（商业化，¥200/月）
```
完整后端架构
├── 独立服务器
├── 微信登录
├── 支付功能
└── 完整数据分析
```

---

## 七、域名推荐（可选）

### 免费域名
- `xxx.pages.dev` - Cloudflare提供
- `xxx.vercel.app` - Vercel提供
- `xxx.netlify.app` - Netlify提供

### 便宜域名（¥20-50/年）
- `.fun` - 便宜好记
- `.online` - 国际通用
- `.xyz` - 便宜注册

### 不推荐的域名
- `.com` - 年费¥60+，起步版不划算
- `.cn` - 需要备案

---

## 八、ICP备案问题

### 什么时候需要备案？
```
使用国内服务器 → 必须备案
使用海外/CDN → 不需要备案
```

### 免费方案的好处
- **不用备案** → 立即上线
- **海外CDN** → 国内访问经过优化
- **绕过监管** → 但需注意合规

### 如果要备案
1. 购买国内服务器（¥200/月起）
2. 备案周期：20-30个工作日
3. 备案后：享受国内直连速度

---

## 九、执行步骤

### 立即行动（今天）
```
1. 注册 GitHub账号 (如果还没有)
2. 创建仓库，上传代码
3. 注册 Cloudflare
4. 部署第一个版本
5. 获得免费域名: xxx.pages.dev
预计耗时: 30分钟
```

### 后续优化（本周）
```
1. 绑定自定义域名（可选）
2. 集成百度OCR API
3. 优化语音识别体验
4. 测试所有功能
```

### 长期运营（下月）
```
1. 收集用户反馈
2. 根据数据迭代优化
3. 考虑商业化变现
```

---

**文档版本**：v1.0  
**更新日期**：2026-05-18  
**预计上线时间**：今天即可！
